{
  "name": "dev-saas",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "jest"
  },
  "dependencies": {
    // ... your existing dependencies (next, react, prisma, etc.)
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.4.2",
    "@testing-library/react": "^14.2.1",
    "@types/jest": "^29.5.12", 
    "jest": "^29.7.0",
    "typescript": "^5.3.3"
    // ... your other existing dev dependencies
  }
}
